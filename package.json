{
  "name": "md-gallery",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Browser-based Markdown reader — poster gallery, search, TOC",
  "scripts": {
    "start": "npx serve .",
    "build": "node scripts/build-release.js",
    "preview": "npm run build && npx serve dist",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "isomorphic-dompurify": "^3.14.0",
    "jspdf": "^4.2.1",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "apca-w3": "^0.1.9",
    "esbuild": "^0.25.0"
  }
}
